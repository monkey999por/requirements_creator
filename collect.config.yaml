# データ収集コマンド設定ファイル
# 各データソースの有効/無効とオプションを管理する

# 生成データの出力先ベースディレクトリ
# data_source/ と requirements/ がこのディレクトリ配下に作成される
output_base_dir: gen

# NewsAPI (https://newsapi.org/)
newsapi:
  enabled: true
  # 環境変数名（.envに定義）
  api_key_env: NEWS_API_KEY
  endpoint: https://newsapi.org/v2/top-headlines
  params:
    country: us
    category: business
    pageSize: 20
  # 出力ファイル名（data_source/yyyy_mm_dd_hh_mm_ss/ 内）
  output_file: news.json

# YouTube Data API (https://developers.google.com/youtube/v3)
youtube:
  enabled: false
  api_key_env: YOUTUBE_API_KEY
  endpoint: https://www.googleapis.com/youtube/v3/videos
  params:
    chart: mostPopular
    regionCode: JP
    maxResults: 20
    # カテゴリID: 0=全体, 1=映画, 10=音楽, 20=ゲーム, 28=科学技術 等
    videoCategoryId: "0"
  output_file: youtube.json

# --- 今後追加予定のデータソース ---

# TikTok
# tiktok:
#   enabled: false
#   api_key_env: TIKTOK_API_KEY
#   endpoint: ""
#   params: {}
#   output_file: tiktok.json

# RSSフィード
# rss:
#   enabled: false
#   feeds:
#     - name: hacker_news
#       url: https://hnrss.org/frontpage
#     - name: techcrunch
#       url: https://techcrunch.com/feed/
#   output_file: rss.json
